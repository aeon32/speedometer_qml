// -*- coding:cp1251 -*-
// -*- кодировка:cp1251 -*-

#ifndef CONFIG_H
#define CONFIG_H

//В файле содержится информация времени конфигурации проекта (при формировании Makefile-ов)

//версия всего проекта, берётся из _build_dir/CMakeLists.txt
#cmakedefine projectVersion     "${PROJECT_VERSION}"
//релиз всего проекта, берётся из _build_dir/CMakeLists.txt
#cmakedefine projectRelease     "${PROJECT_RELEASE}"
//тип сборки проекта, берётся из _build_dir/CMakeLists.txt (варианты DEVELOP, DISTRIB, WORK)
#cmakedefine projectBuildType  "${PROJECT_BUILD_TYPE}"


#endif
